{equation}
    Q = XW_Q
    K = XW_K
    V = XW_V
    \[
    \text{scores} = \frac{QK^T}{\sqrt{d_k}} \in \mathbb{R}^{n \times n}
    \]
    attn_weights = softmax(scores)
    output = attn_weights × V
{/equation}